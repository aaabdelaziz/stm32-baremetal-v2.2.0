# stm32-baremetal-v2.2.0
stm32 bare metal projects dedicated for every separate peripheral showing steps and expalantions from IDE configuration steps, include paths, till register setting per peripheral then debgguging and testing output
